import styled from 'styled-components'

const Wrapper = styled.div`
  font-size: 1rem;
  margin: 0.5rem 1rem;
`

const Question = styled.div`
  font-size: 1rem;
  font-weight: bold;
`

export { Wrapper, Question }
